const WRAP = '/wrap';
const WRAP_FINALIZE = '/wrap/:transactionHash';
const UNWRAP = '/unwrap';
const UNWRAP_FINALIZE = '/unwrap_finalize';

export const paths = {
  WRAP,
  WRAP_FINALIZE,
  UNWRAP,
  UNWRAP_FINALIZE,
};
